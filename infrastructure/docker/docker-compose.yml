services:
  postgres:
    image: postgres:17
    container_name: humanos-postgres
    environment:
      POSTGRES_DB: humanos
      POSTGRES_USER: humanos
      POSTGRES_PASSWORD: humanos
    ports:
      - "5432:5432"
    volumes:
      - humanos-data:/var/lib/postgresql/data

volumes:
  humanos-data: